name: 🕵 Pull Request Automation

on:
  pull_request:
    paths-ignore:
      - '**/*.md'

permissions:
  contents: write
  pull-requests: write

jobs:
  coverage:
    name: PR Validation
    uses: asincode-sas/actions/.github/workflows/test-node.yaml@main
    with:
      ref: ${{ github.ref }}
    secrets: inherit
